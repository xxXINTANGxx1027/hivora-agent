# WORKPLAN — 可分派的工作包

把剩下的活拆成互相独立的包，每个包一个 agent 能独立做完。
**开工前先读 [AGENTS.md](AGENTS.md)** —— 那里是铁律，这里只是任务书。

---

## 给接手的 agent：三条硬规则

1. **测试必须全绿才算做完**
   ```bash
   cd server && .venv/bin/python -m pytest tests -q            # 含浏览器冒烟
   ```
   改了行为就补测试。改坏了别改测试去迁就代码 —— 先想清楚是谁错了。

2. **改前端只改 `server/static/index.html`**，改完必须
   ```bash
   ./sync-frontend.sh && ./sync-frontend.sh --check
   ```
   `frontend/index.html` 是生成物。三个 repo 都装了 pre-push 钩子，不同步会拒绝 push。

3. **不要 push**。提交可以，推送由项目所有者用 `./push.sh` 执行 —— push 会直接部署到生产。

---

## 并行车道

三个包同时开工是安全的，前提是不要跨车道改文件。

| 车道 | 独占的文件 | 包 |
|---|---|---|
| **A · 客户端 SPA** | `server/static/index.html` | WP-2, WP-6 |
| **B · 后端检索** | `knowledge.py`, `db.py` 的 Chunk | WP-3 |
| **C · 后端业务** | `main.py` 的管理段, `admin/index.html` | WP-1, WP-4 |

> ⚠️ **客户端是单文件 SPA（1300+ 行）。两个 agent 同时改它必冲突。**
> A 车道一次只能有一个 agent。WP-2 和 WP-6 要串行，或者由同一个 agent 一起做。

依赖：

```
WP-1 开通邮件 ─┐
WP-2 开通引导 ─┴─→ 可以开始发付费账号
WP-3 向量检索 ←── 需要先有真实条款 PDF（只有所有者能拿到）
WP-4 定价账单 ←── 需要先定价（商业决策，不是技术任务）
WP-5 / WP-6 ─── 无依赖，随时可做
```

---

## ✅ WP-1 · 开通邮件（已完成）

**为什么**：开通流程是「客户买配套 → admin 建账号 → 发 email 让他登录」，
中间发信这步现在完全没有，密码要人工复制发过去。这是收钱之后的第一印象。

**做什么**
- 新增 `server/email.py`：用标准库 `smtplib`，不引第三方依赖。
  没配 SMTP 时**整个模块降级为 no-op**，绝不能因为发信失败让建账号失败。
- `POST /api/admin/agents/create` 建完账号后发一封欢迎信（登录地址、账号、密码、下一步做什么）。
  响应加 `email_sent: bool`。
- `POST /api/admin/agents/{id}/password` 重置后同样可选发信。
- 管理站显示这封信发出去没有；**没发出去时仍然要把密码显示出来**让 admin 手动发。

**环境变量**（都写进 `.env.example` 和 `render.yaml`）
```
SMTP_HOST / SMTP_PORT / SMTP_USER / SMTP_PASSWORD / SMTP_FROM
APP_LOGIN_URL     # 信里给的登录地址
```

**验收**
- 没配 SMTP 时建账号照常成功，`email_sent=false`，日志有一条 warning
- 配了但发信抛异常时，账号**依然建成功**（有测试覆盖）
- 信里不能出现 token、连接串这类东西；密码是明文（这是发给本人的开通信，可接受）
- 用假 SMTP 服务器测，不许真发信

**不要做**：不要引入 SendGrid/Resend 之类的 SDK。SMTP 通用，任何服务商都能接。

---

## ✅ WP-2 · 开通引导（已完成）

**为什么**：新账号登进来是空的 —— 0 客户、0 产品、0 条款。
代理人看到全空很可能当天就流失。这是目前最大的激活风险。

**做什么**
- 新增 `GET /api/onboarding`：返回四步的完成情况
  ```json
  {"steps":[{"key":"docs","done":false,"count":0}, ...],"done":false}
  ```
  四步：上传条款文档 → 加产品 → 连接 Telegram → 加第一个客户。
- 客户端「总览」页在未全部完成时，把引导卡片放在最上面，每步一个直达按钮。
  全部完成后自动消失，不要留一个空壳。
- 文案中英双语（`I18N` 字典两边都要加，`AGENTS.md` 铁律）。

**验收**
- 空账号看到四步全未完成；每完成一项对应步骤变勾
- 四项都完成后卡片消失
- 加一条浏览器冒烟：新账号登录能看到引导，点某一步能跳到对应页面

**不要做**：不要做强制引导（挡住界面的那种）。代理人可能只想先看看。

---

## WP-3 · 向量检索（pgvector）

**为什么**：条款问答是产品的头号卖点，现在是中文 2-gram 关键词打分。
真实的一百页条款 PDF 上召回会明显变差。

**前置**：需要至少一份**真实条款 PDF** 才能评估效果。没有就先别做 —— 会变成
拿假数据调参。

**做什么**
- `db.Chunk` 加 embedding 列；Neon 支持 pgvector，本地 SQLite 要能降级
  （查不到 pgvector 就回退现在的关键词打分，**不能让本地开发和 CI 跑不起来**）。
- 上传文档时算 embedding；检索改成向量相似度 + 关键词的混合召回。
- embedding 来源要可配置。注意成本 —— 记账要跟现有的 `db.record_usage` 打通。

**验收**
- 没有 pgvector 的环境（本地 SQLite / CI）自动回退，全部测试仍然绿
- 有一组"真实提问 → 期望命中的条款页"的用例，新老两种检索都跑，新的召回不能更差
- 大文档上传不能把内存打爆（现在有 `_MAX_CANDS=800` 的兜底）

**不要做**：不要为了向量检索去掉「查不到就说查不到」。宁可召回为空也不能编。

---

## WP-4 · 定价与账单

**前置**：先定价 —— 这是商业决策，不是技术任务。定完再开工。

**为什么**：`plan` 现在是个自由文本字段。用量和成本已经能按账号看了
（管理站「用量与成本」），有定价依据了，但没有价格、额度档位和账单。

**做什么**
- 套餐做成有定义的档位（名称、月费、token 额度、能开几个 Telegram bot 之类）
- 管理站按档位设置，而不是手打 `paid` / `trial`
- 账单周期与到期日联动（`expires` 已有，登录和每个请求都会校验）

**不要做**：先别接支付网关。手工开票 + 管理站标记到期，够跑前几十个客户。

---

## WP-5 · 分页与回收站清理

**为什么**：列表现在硬上限 500 条（截断会打日志，不静默丢数据），短期够用。
回收站只标记不真删，缺一个到期清理。

**做什么**
- 客户/预约/续保/审计加真分页（`limit` + `offset` + 总数），前端加翻页
- 一个定时任务把超过 `TRASH_KEEP_DAYS` 的软删行真删掉；
  **PDPA 相关的硬删已经有了**（`/api/admin/clients/{id}/purge`），别重复实现

**验收**：翻页在有 1000+ 条数据时正确；清理任务有 dry-run 模式

---

## WP-6 · 删掉客户视角模拟器

**为什么**：`#cust-overlay` 那个 WhatsApp 手机壳界面从 `f007af5` 起就没有入口了，
现在有真实 Telegram 客户更是没用。约 50 行 JS + 一段浮层标记。

**做什么**：删掉 `#cust-overlay` 相关的 HTML/CSS/JS
（`openCustomer` `closeCustomer` `loadCustomer` `custSend` `sheetCust` 和 i18n 里的 `cust_view`）。

**验收**：删完 `grep -c "cust-overlay\|openCustomer" server/static/index.html` 为 0，
浏览器冒烟全绿。

> ⚠️ **这是删功能，动手前要项目所有者确认。**

---

## 只有项目所有者能做的（agent 做不了）

这些不是工作包，是外部动作 —— 列在这里是因为它们卡着上面的验收。

| 事 | 卡住什么 |
|---|---|
| `./push.sh` 把 8 个提交推上去 | 线上还是旧版，所有线上验证都在测旧代码 |
| Render 加 `PUBLIC_BASE_URL` | Telegram 客户渠道完全不能用 |
| 建 `hivora-admin` repo + Vercel 项目 | 管理站没上线 |
| 拿到真实条款 PDF | WP-3 无法开始；产品头号卖点是空的 |
| Render / Neon 升到付费档 | 冷启动几十秒，代理人在客户面前打不开 |
| 配 `SENTRY_DSN`、备份 secret | 线上出事你不会知道；备份工作流没在跑 |
| 找 3 个真代理人试用 | **唯一能验证产品是否成立的动作** |
